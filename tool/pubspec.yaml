name: tool
description: A simple command-line application.
version: 1.0.0
# homepage: https://www.example.com

publish_to: none

environment:
    sdk: ">=2.16.1 <3.0.0"

# dependencies:
#   path: ^1.8.0

dependencies:
    args: ^2.3.0
    chapturn_sources:
        path: ..

dev_dependencies:
    lints: ^1.0.0
