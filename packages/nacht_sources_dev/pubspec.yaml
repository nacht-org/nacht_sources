name: nacht_sources_dev
description: A starting point for Dart libraries or applications.
version: 1.0.0
# homepage: https://www.example.com

publish_to: none

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  build:
  source_gen:
  nacht_sources_annotations:
    path: ../nacht_sources_annotations
  glob: ^2.0.2

dev_dependencies:
  lints: ^1.0.0
  test: ^1.16.0

  build_runner:
  build_test:
  build_resolvers: ^2.0.6
