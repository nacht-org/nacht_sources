name: generators
description: A starting point for Dart libraries or applications.
version: 1.0.0
# homepage: https://www.example.com

publish_to: none

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  build:
  source_gen:
  annotations:
    path: ../annotations/

dev_dependencies:
  lints: ^1.0.0
  test: ^1.16.0

  build_runner:
  build_test:
